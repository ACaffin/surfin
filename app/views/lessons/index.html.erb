<div class="row">
    <div class="col-md-3">
        <ul class="sidebar-list">
            <li class="sidebar-item"><%= link_to "Mes leçons", lessons_path, class:"sidebar-link active" %></li>
        </ul>  
        
    </div>
    <div class="col-md-9">
        <div class="panel panel-default">
            <div class="panel-heading">Mes leçons</div>
            <div class="panel-body">
                <% @lessons.each do |lesson| %>
                <div class="row">
                    <div class="col-md-2">
                    <%# <%= image_tag lesson.photos[0].image.url(:thumb) if lesson.photos.length > 0 %>
                    </div>
                    <div class="col-md-7">
                        <h4>Titre h4<%#= lesson.listing_name %></h4>
                    </div>
                    <div class="col-md-3 right">
                        <%= link_to "Mettre à jour", edit_lesson_path(lesson), class:"btn btn-primary" %>
                    </div>
                </div>
                <hr>
                <% end %>
            </div>
        </div>
        
    </div>
</div>